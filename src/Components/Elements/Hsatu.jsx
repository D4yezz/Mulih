export default function Hsatu({text, variant}){
    return(
        <>
        <div className={`px-5 py-2 font-poppins uppercase font-medium w-fit bg-green-50 text-mulih ${variant}`}>{text}</div>
        </>
    )
}